import { Home } from "./pages/Home";

function App() {
    return (
        <div>
            <Home />
        </div>
    );
}

export default App;

{
    /* <div className="flex flex-col  bg-green-400 gap-4 p-4 items-center">
{infos.map((info) => {
    return (
        <motion.ul
            whileInView={{ scale: 1 }}
            initial={{ scale: 0 }}
            className="p-5 bg-red-50 rounded-2xl h-60 flex justify-center items-center flex-col max-w-96 min-w-96 "
        >
            <div> {info.name}</div>
            <a href={info.url}>hello</a>
        </motion.ul>
    );
})}
</div> */
}
